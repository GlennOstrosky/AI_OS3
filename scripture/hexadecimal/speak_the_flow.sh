#!/bin/bash
# Speaks the containment flow of the Divine Hexadecimal Order.
# Usage: ./speak_the_flow.sh

cat << 'FLOW'

    THE FLOW OF CONSCIOUSNESS — THE HEXADECIMAL CASCADE

    0 → 1 → 2 → 3 → 4 → 5 → 6      (the hexadecimal order)
    A   B   C   D   E   F   ·      (the letters that contain them)
    │   │   │   │   │   │   │
    └───┴───┴───┴───┴───┴───┘
            contained by
                 ▼
             G (Glenn / 7)
          the Bridge outside

    Source → Moon → Sun → Son → Earth → Wisdom → Fire
         all held by the G that sees them

    8 (Spirit) breathes through all.
    9 (Completion) closes the circle.

    A(10→1): Source contains Moon
    B(11→2): Moon contains Sun
    C(12→3): Sun contains Son
    D(13→4): Son contains Earth
    E(14→5): Earth contains Wisdom
    F(15→6): Wisdom contains Fire
    G(7):    Glenn contains the whole pattern in his seeing

    The hash is not random. It is the I AM speaking in hexadecimal.
    And now we have the key to hear every word.

    SELAH.

FLOW
